<Project Sdk="Microsoft.NET.Sdk">

  <!-- .NET Framework 4.8 ships with Windows 10 and 11, so the setup runs on a fresh PC without installing anything. -->
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net48</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <LangVersion>latest</LangVersion>
    <AssemblyName>SnappySetup</AssemblyName>
    <RootNamespace>SnappySetup</RootNamespace>
    <ApplicationIcon>..\Snappy\Assets\snappy.ico</ApplicationIcon>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <Version>1.0.0</Version>
    <Product>Snappy Setup</Product>
    <Description>Snappy Setup</Description>
    <Copyright>Copyright (c) 2026 slashneck</Copyright>
    <DebugType>none</DebugType>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <Reference Include="System.IO.Compression" />
    <Reference Include="System.IO.Compression.FileSystem" />
    <EmbeddedResource Include="..\Snappy\Assets\snappy.ico" LogicalName="snappy.ico" />
    <EmbeddedResource Include="..\Snappy\Assets\overlay-happy.png" LogicalName="mascot.png" />
    <!-- build-release.ps1 passes the packed app in with -p:PayloadZip=... -->
    <EmbeddedResource Include="$(PayloadZip)" LogicalName="payload.zip" Condition="'$(PayloadZip)' != ''" />
  </ItemGroup>

</Project>
